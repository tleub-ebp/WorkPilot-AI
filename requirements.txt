# ============================================================
# WorkPilot AI — Root Requirements (meta-package)
# ============================================================
# Les dépendances réelles sont dans apps/backend/requirements.txt.
# Ce fichier référence le backend pour permettre un `pip install -r requirements.txt`
# depuis la racine du projet sans confusion.
# ============================================================

-r apps/backend/requirements.txt
