# ============================================================
# WorkPilot AI — Root Requirements (meta-package)
# ============================================================
# Les dépendances réelles sont déclarées dans apps/backend/requirements.txt.
# Ce fichier les inclut via un -r pour que `pip install -r requirements.txt`
# au repo root fonctionne (sinon les nouveaux contributeurs et CI installent
# silencieusement zéro dépendance).
# ============================================================

-r apps/backend/requirements.txt
